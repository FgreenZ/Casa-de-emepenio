package controllers;
import views.*;
//import models.Cliente;
import java.util.ArrayList;

public class MainController {
	/*
    private MainFrame ventanaPrincipal;
    private AuthView vistaLogin;
    private HomeView vistaHome;
    
    // Aquí vive tu base de datos ahora (Modelo)
    private ArrayList<String[]> baseDatosClientes;

    public MainController(MainFrame frame, AuthView login, HomeView home) {
        this.ventanaPrincipal = frame;
        this.vistaLogin = login;
        this.vistaHome = home;
        this.baseDatosClientes = new ArrayList<>();

        configurarEventos();
    }

    private void configurarEventos() {
        // Al hacer clic en ingresar en el login
        vistaLogin.getBtnIngresar().addActionListener(e -> {
            // Lógica de validación...
            ventanaPrincipal.mostrarVista("HOME");
        });

        // Al hacer clic en salir en el home
        vistaHome.getBtnSalir().addActionListener(e -> {
            ventanaPrincipal.mostrarVista("LOGIN");
        });
    }
    */
}